# SkillStack

A developer project management & learning tracker built with FastAPI and PostgreSQL.

## Features
- User authentication (JWT)
- Project and Task CRUD
- Role-based permissions
- Progress tracking
- Analytics (later)

## Setup
```bash
pip install -r requirements.txt
uvicorn app.main:app --reload
